services:
  ghost-box:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghost-box
    working_dir: /workspace
    volumes:
      - .:/workspace
      - ghost-box-home:/home/node
    stdin_open: true
    tty: true

volumes:
  ghost-box-home:
